WALLETCONNECT_PROJECT_ID=
# Optional: Alchemy API key used for ethereum/base/bnb/sepolia/baseSepolia RPCs.
# If empty, the UI falls back to public RPCs. HyperEVM always uses the official endpoint.
ALCHEMY_API_KEY=
